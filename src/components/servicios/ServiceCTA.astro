---
import SectionTitle from "./SectionTitle.astro";
import ServiceForm from "./ServiceForm.astro";

export interface Props {
  title: string;
  preselectedService?: string;
}

const { title, preselectedService = "" } = Astro.props;
---

<section class="py-16 md:py-24 px-4 relative overflow-hidden" style="background: linear-gradient(180deg, #334155 0%, #101417 100%)">
  <!-- Imagen izquierda - esquina inferior -->
  <img 
    src="/img/figuea2-Servicios.png" 
    alt="" 
    class="absolute left-[5%] bottom-[5%] h-[50%] max-w-[20%] w-auto object-contain object-bottom pointer-events-none select-none z-0 opacity-40 hidden lg:block"
  />

  <!-- Imagen derecha - esquina superior -->
  <img 
    src="/img/figura_formularioServ.png" 
    alt="" 
    class="absolute right-[5%] top-[10%] h-[55%] max-w-[22%] w-auto object-contain object-top pointer-events-none select-none z-0 opacity-40 hidden lg:block"
  />
  
  <div class="max-w-3xl mx-auto text-center relative z-10">
    <SectionTitle title={title} variant="white" animate={true} />
    <p class="text-neutral-300 mb-10" data-scroll="fade" data-scroll-delay="100">
      Cuéntanos tu proyecto y te prepararemos una propuesta sin compromiso en menos de 48 horas.
    </p>
  </div>
  <div class="max-w-2xl mx-auto relative z-10">
    <ServiceForm preselectedService={preselectedService} />
  </div>
</section>